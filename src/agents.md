# Src Domain Constraints

- Use `src/lib` for shared logic.
- UI components live in `src/components`.
- App routes in `src/app`.
- Always check `src/db/schema.ts` for data definitions.
- Keep components small and specialized.
