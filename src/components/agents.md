# Components Domain Constraints

- Atomic UI components in `src/components`.
- Sections in `src/components/sections`.
- Shop-specific logic in `src/components/shop`.
- Prefer composition over large, monolithic components.
- Use existing context providers (e.g., `cart.tsx`).
